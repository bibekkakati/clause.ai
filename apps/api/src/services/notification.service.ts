/**
 * Sends email notification to users.
 */
export const sendEmailNotification = async (
    templateId: string,
    email: string,
    data: Record<string, any>,
) => {
    // TODO: To be implemented
    // Connect to mail server
    // Create and map template
};
